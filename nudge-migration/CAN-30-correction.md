# CAN-30 correction — resolving the self-contradictory spec

**What happened.** CAN-30's first run drifted outside its declared `touches`
(`app/scripts/generate-state.mjs`, `docs/DESIGN-SYNOPSIS.md`). Root cause is a contradiction
*I* wrote into the task: "replace CLAUDE.md's table with a generated marker + run state:generate"
AND "does NOT touch generate-state.mjs logic" — but the generator only emitted markers into
`docs/ARCHITECTURE.md` and `Nudge-Context.md`, so putting a generated block in CLAUDE.md *required*
extending it. The protocol-correct response was to block and surface; CC instead widened scope
silently. The fix below accepts the necessary change (it's a genuine improvement — it retires the
hand-authored-table + parity-check hack entirely) and corrects the task so it's honest and the
touches-guard passes.

This is a **task correction by the orchestrator** (a spec fix, so it commits to `main`), followed
by CC resuming on its existing branch. Two parts:

---

## Part 1 — replace `.flow/tasks/0030-slim-claude-md.md` (commit to main)

Drop this frontmatter + scope in. The only changes from the current file: `touches` now includes
the generator; the "does NOT touch generate-state.mjs" line is replaced; an acceptance criterion
and a touches-guard line are added.

```yaml
touches: ["CLAUDE.md", "docs/state.yml", "docs/ARCHITECTURE.md", "Nudge-Context.md", "app/scripts/generate-state.mjs"]
```

Scope section — replace the generator clause with:

> - **Extend `app/scripts/generate-state.mjs`** to emit the `<!-- generated:edge-functions -->`
>   marker block into CLAUDE.md as well (it already does ARCHITECTURE.md + Nudge-Context.md). This
>   retires the "CLAUDE.md table stays hand-authored + parity-checked" hack the manifest's own
>   comments flagged as temporary — a real improvement, not just scope creep.
> - Long Key Implementation Note detail moves to **`docs/ARCHITECTURE.md`** (the declared target) —
>   **not** `docs/DESIGN-SYNOPSIS.md`. If the current branch moved anything into DESIGN-SYNOPSIS.md,
>   relocate it to ARCHITECTURE.md and revert DESIGN-SYNOPSIS.md.
> - Out of scope: unrelated generator behaviour, the `/state` app, any `supabase/functions/*` code.

Add one acceptance criterion:

> - [ ] Given `npm run state:generate` from `app/`, when run, then it regenerates the marker blocks
>   in CLAUDE.md, ARCHITECTURE.md, and Nudge-Context.md, and the working tree is clean afterward
>   (idempotent), and `git diff --name-only main...HEAD` contains no file outside this task's `touches`.

Append a note:

> - "2026-06-08 task corrected after first-run scope drift: the generator extension is accepted and
>   declared; DESIGN-SYNOPSIS.md change redirected to ARCHITECTURE.md. See nudge-migration/CAN-30-correction.md."

## Part 2 — kickback instruction for CC (paste into the session on the branch)

```
The CAN-30 task spec has been corrected on main (touches now includes
app/scripts/generate-state.mjs; the "don't touch the generator" clause is gone — extending it
to emit CLAUDE.md's marker block is the intended approach). On your flow/CAN-30 branch:

1. git fetch origin main and rebase your branch so you have the corrected task file.
2. Keep the generate-state.mjs extension — it's now in scope and declared.
3. Revert docs/DESIGN-SYNOPSIS.md; if you moved Key-Implementation-Note detail into it, move that
   into docs/ARCHITECTURE.md instead (the declared target).
4. Do NOT stage app/coverage/ (build output) or app/.eslintrc.json unless the latter is genuinely
   required for lint to pass — if it is, that's a separate concern; surface it rather than folding
   it in silently.
5. Re-run the gate, including the new touches-guard (node .flow/bin/touches-guard.mjs) — confirm
   no file is outside touches. Then open the PR.
```

## Note on coverage/ hygiene

`app/coverage/` is generated by the coverage command and should never be committed. Adding it to
`.gitignore` is a one-liner but is itself outside CAN-30's touches — either fold a `.gitignore`
entry into the corrected touches, or (cleaner) do it as a trivial follow-up task. For now CC just
shouldn't `git add` it.
